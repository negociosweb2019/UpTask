<!DOCTYPE html>
html(lang="en")
    head
        include partials/header

    body.login
        .contenedor-formulario
            h1 #{nombrePagina}
            p Ingresa tu nueva contraseña

            form(method="POST" class="caja caja-login")
                .campo
                    label(for="password") Contraseña
                    input(type="password" name="password" placeholder="Ingresa tu nueva contraseña")
                .campo.enviar   
                    input(type="submit" value="Establecer la contraseña" class="boton")