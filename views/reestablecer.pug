<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 #{nombrePagina}
            p Ingresa tu correo electrónico para reestablecer tu contraseña

            //- Mostrando los errores de reestablecer contraseña
            if locals.mensajes
                each categoria in Object.keys(locals.mensajes)
                    each error in mensajes[categoria]
                        .alerta(class=`${categoria}`)
                            p #{error}

            form(action="/reestablecer" method="POST" class="caja caja-login")
                .campo
                    label(for="email") Correo electrónico
                    input(type="text" name="email" placeholder="Ingresa tu correo electrónico")
                .campo.enviar
                    input(type="submit" value="Reestablecer la contraseña" class="boton")
                .campo
                    a(href="/iniciar_sesion") ¡Volver a iniciar sesión!